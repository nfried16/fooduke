import React from 'react';
import { withRouter } from 'react-router-dom';

const About = props => {

	return (
		<div>
            we made a fod point calculator
		</div>
	);
}

export default withRouter(About);